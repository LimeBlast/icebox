<%= semantic_form_for @register_user_form, url: users_path do |f| %>
  <%= f.inputs :email, :password, :password_confirmation %>

  <%= f.actions do %>
    <%= f.action :submit, :label => 'Sign up' %>
    <%= f.action :cancel, :label => 'Cancel' %>
  <% end %>
<% end %>
